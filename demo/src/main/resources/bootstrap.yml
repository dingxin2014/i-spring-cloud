server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
    healthcheck:
      enable: true


spring:
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:7000/
      username: admin
      password: admin
